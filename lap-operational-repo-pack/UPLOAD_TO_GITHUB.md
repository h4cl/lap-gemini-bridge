# Upload Instructions

Copy these folders/files into the root of the GitHub repo:

```text
README_FIRST.md
docs/
templates/
session_logs/
telemetry/
experiments/
audits/
protocols/
schemas/
```

Commit message:

```text
add LAP operational repository structure
```

Render does not need these files to run the server, but they turn the repo into the operational knowledge base for LAP.
